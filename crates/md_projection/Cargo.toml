[package]
name = "md_projection"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
license = "GPL-3.0-or-later"

[lints]
workspace = true

[lib]
path = "src/lib.rs"
doctest = false

[features]
test-support = []

[dependencies]
markdown_wysiwyg.workspace = true
md_buffer.workspace = true
md_text.workspace = true

[dev-dependencies]
md_buffer = { workspace = true, features = ["test-support"] }
