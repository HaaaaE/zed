# Markdown Editor 滚动性能目标

## 背景

300KB Markdown 比短 Markdown 滑动更慢，主要原因不是每次滑动都处理整篇文档。当前编辑器只处理屏幕附近的 source row。

真正的问题是：短文档很快就把所有行都计算过了；300KB 文档在滑动过程中会不断遇到还没有计算过的行。每个新行第一次进入屏幕附近时，都要取文本、计算显示内容、计算字体排版、计算高度并创建 UI 元素。

如果内存不是主要问题，优化方向应当是：尽量提前保存这些计算结果，让滑动过程少做第一次计算。同时也要先处理不需要明显增加内存的重复复制问题。

## 全局约束

分批预计算不能无条件占用主线程。它必须满足下面约束。

### 不能影响输入和当前帧绘制

当前屏幕需要的行优先级最高。用户输入、选择、光标移动、鼠标拖选和当前帧绘制不能等待远处行的预计算。

预计算只能在当前帧必要工作完成后继续推进。每次推进必须有时间上限。

建议起始值：

- 正在滚动或正在输入时：每帧最多 `1ms`。
- 没有输入且没有滚动时：每帧最多 `2ms`。
- 单帧最多处理 `64` 行。
- 如果 `1ms` 或 `2ms` 先用完，就停止本帧预计算。

这些数字不是最终结论，需要用 perf 和手感调整。

### 必须可以取消

每一批预计算都要绑定这些条件：

- buffer version
- editor mode
- wrap width
- row style
- Rendered 模式下影响 marker 显隐的 active source range

只要其中任何一个变化，旧任务不能继续写入缓存。

### 必须按优先级计算

预计算顺序不应该简单从第 0 行算到最后一行。

建议顺序：

1. 当前屏幕内的行。
2. 当前屏幕下方一段距离的行。
3. 当前屏幕上方一段距离的行。
4. 用户滚动方向上的更远行。
5. 其他行。

如果用户改变滚动方向，预计算方向也要更新。

### 不能在列表 layout / render 过程中触发列表重测

预计算可以更新编辑器自己的缓存，但不能在 GPUI list 正在 layout 或 render 时直接调用 list remeasure。

如果某批预计算发现行高度变化，需要把行号记录下来，在下一帧或明确的 deferred 回调里批量通知列表。

### 宽度相关结果只对当前宽度有效

行显示数据可以分两类：

- 宽度无关：显示文本、样式范围、未换行排版结果。
- 宽度相关：换行结果、视觉行范围、真实行高。

窗口宽度变化时，宽度相关结果必须失效。宽度无关结果可以继续保留。

### 必须有大文件保护

即使当前认为内存不是主要问题，也不能对任意大小文件无限预计算。

建议起始策略：

- `<= 1MB` 且 `<= 20,000` 行：允许整篇文档分批预计算显示数据和当前宽度下的高度。
- `1MB - 5MB` 或 `20,000 - 100,000` 行：优先预计算高度和屏幕附近的布局，整篇预计算降低优先级。
- `> 5MB` 或 `> 100,000` 行：只预计算屏幕附近和滚动方向上的行，不做整篇主动预计算。

这些阈值也需要实测后调整。

## 执行顺序

### 0. 先补齐短文档与长文档的对照测量

目的：先确认每一步优化到底减少了什么成本，避免只看 300KB 单组数据。

需要新增或调整 perf 用例：

- 短 Markdown 滑动：例如 5KB 左右。
- 300KB Markdown 滑动：保留当前大文档用例。
- 第一次滑动：从未显示过的区域开始滑。
- 第二次滑动：同一段区域已经显示过后再滑一次。
- Source 模式和 Rendered 模式都要覆盖。

需要记录的内部计数：

- 新建了多少个 display row。
- 新建了多少个 row layout input。
- 新建了多少个当前宽度下的 row layout。
- 调用了多少次文本排版。
- Rendered 模式下查了多少次 Markdown block / inline span。

预期判断：

- 如果第一次滑动慢、第二次滑动快，说明主要问题是新行第一次计算。
- 如果第二次滑动仍然慢，说明渲染阶段仍有大量重复复制或重复创建 UI。

### 1. 减少缓存命中后的复制

这是优先级最高的无明显内存增长优化。

当前问题：

- 行布局缓存命中后，仍然会复制整份布局结果。
- 渲染每条视觉行时，会复制 `DisplayRow` 给鼠标事件回调。
- `visual_rows` 会被复制后再遍历。

目标：

- 缓存中的布局结果用共享引用保存，例如 `Arc<DisplayRowLayout>`。
- 渲染函数尽量接收引用，而不是接收完整布局副本。
- 鼠标事件只保存必要信息，例如 row、source range、visual row index、display range，不复制整个 `DisplayRow`。
- 遍历 `visual_rows` 时使用引用。

完成标准：

- 滑动已经显示过的区域时，布局缓存命中不再伴随大对象复制。
- 鼠标命中、选择、光标、Home / End、上下移动行为保持不变。

### 2. Source 模式去掉重复字符串复制

这是另一个优先级很高的无明显内存增长优化。

当前问题：

- Source row 已经有 `display_row.text`。
- `source_display_fragments` 又把同一段文本复制到 fragment 里。
- 渲染视觉行时还会为文本片段创建新的 `String`。

目标：

- 普通文本 fragment 不保存 owned text，只保存 display range 和 style。
- 渲染时从 `display_row.text` 按范围取文本。
- 只有在交给 GPUI 文本元素时才创建最终需要的 owned text。

完成标准：

- Source 模式普通文本行不再在 layout input 阶段复制整行文本。
- Source 模式的纯文本路径仍然不刷新 Markdown syntax tree。
- 现有 Source selection、caret、mouse hit testing、wrapped movement 测试继续通过。

### 3. 分批预计算 Source 行的显示数据

这是第一项明确用内存换滑动计算的优化。

当前问题：

- 300KB 文档滑动时，新进入屏幕附近的 Source 行才开始生成 display row 和 layout input。
- 短文档很快全部算过，长文档会持续遇到未计算行。

目标：

- 打开文档后先计算首屏附近的 Source 行。
- 然后在后续帧中分批计算更多行。
- 保存每行的 display row 和宽度无关 layout input。
- 不要一次性计算整篇文档，避免打开文件时卡顿。

建议策略：

- 使用“全局约束”里的时间上限和行数上限。
- 优先计算当前屏幕下方的行，再计算上方和更远的行。
- 用户开始滚动时，优先计算滚动方向上的行。

失效规则：

- 单行 Source 编辑只重新计算被编辑行。
- 长度不变的单行编辑可以保留后续行的缓存。
- 长度变化的编辑需要谨慎处理后续行的 source range 偏移。
- 跨行编辑、Rendered 模式编辑、row count 变化继续走保守失效。

完成标准：

- 300KB Source 模式第一次向下滑动时，新建 display row 和 layout input 的数量明显下降。
- 第二次滑动同一区域时，Source 模式滑动耗时接近短文档。

### 4. 保存当前窗口宽度下的行高度

这是对滚动稳定性影响最大的内存型优化。

当前问题：

- 列表一开始只知道默认行高。
- 真实行高要等行被显示和测量后才知道。
- 300KB 文档有大量未测量行，所以滑动时会持续修正总高度和滚动位置。

目标：

- 保存每行在当前窗口宽度下的真实高度。
- 保存每行换成了几条视觉行。
- 保存每条视觉行对应的文本范围。
- 让列表在行还没有真正显示前，也能拿到更准确的高度提示。

需要的支持：

- GPUI `ListState` 当前只有统一默认高度提示。
- 需要增加按 item 设置高度提示的能力，或者在 `md_editor` 中维护一层可供 list 使用的高度信息。

失效规则：

- 窗口宽度变化后，当前宽度下的高度缓存失效。
- 字体大小、行高、主题中影响高度的设置变化后，高度缓存失效。
- Inline image / formula 等最终尺寸变化时，只重新计算受影响行。

完成标准：

- 300KB 文档滚动过程中，滚动条和滚动位置不再因为大量未测量行而持续明显修正。
- Source 和 Rendered 的 scroll perf 中，第一次滑动与第二次滑动差距缩小。

### 5. Rendered 模式合并每行 Markdown 查询

这是 Rendered 模式的主要后续优化。

当前问题：

同一个 Rendered 行可能分别做这些查询：

- 判断是不是标题。
- 计算哪些 Markdown 标记要隐藏。
- 收集加粗、斜体、代码、链接等样式。
- 查找 inline image 和 inline math。
- 判断是否是 block image 或 block formula。

这些查询本身不是全篇扫描，但同一行重复查询会增加新行第一次显示的成本。

目标：

- 为每个 Rendered 行生成一次行分析结果。
- 行分析结果包含 block kind、heading level、hidden ranges、style ranges、inline atoms、block descriptor。
- display row、layout input、block layout 都复用这份结果。

完成标准：

- Rendered 模式每个新行的 Markdown block / inline span 查询次数减少。
- Rendered 模式大文档第一次滑动耗时下降。
- Marker reveal / hide、active source range、selection 相关行为保持正确。

### 6. 重新评估屏幕外额外处理范围

当前编辑器会处理屏幕上下额外区域，目的是减少快速滚动时的空白或高度突变。

如果第 3、4 步完成，额外区域可以继续保持较大，因为大部分数据已经提前算好。

如果第 3、4 步暂时不做，可以考虑减小这个范围，以减少每次滑动时处理的新行数量。

完成标准：

- 快速滚动没有明显空白。
- 每次滚动要新计算的行数下降。
- 短文档和 300KB 文档的滑动差距缩小。

### 7. 最后再考虑缓存上限

如果内存不是主要问题，暂时不要优先做缓存淘汰。

原因：

- 当前目标是让长文档滑动少做重复计算。
- 太早限制缓存，会让用户往回滑时再次支付计算成本。

只有在实际内存数据证明缓存过大时，再考虑：

- 只保留当前屏幕附近和最近访问过的行。
- 对 Source 和 Rendered 分别设置缓存上限。
- 保留行高度，淘汰更重的排版结果。

## 不建议优先做的方案

### 不建议直接让列表测量所有行

GPUI list 可以测量所有 item，但这里不适合作为主方案。

原因：

- 它会创建并 layout 所有行的 UI 元素。
- 300KB 文档会把大量工作集中到打开文件或切换模式时。
- 这会把滑动卡顿转移成打开文件卡顿。

更好的做法是分批预计算轻量数据和行高度，不一次性创建所有 UI 元素。

### 不建议缓存完整 UI 元素

完整 UI 元素包含鼠标事件、选区、光标、当前行背景等状态。

这些状态会变化，直接缓存完整 UI 元素容易产生过期状态。

应该缓存渲染所需的数据，而不是缓存已经创建好的 UI 元素。

## 验证标准

每个阶段完成后都要运行：

- `cargo fmt -p md_editor`
- `cargo check -p md_editor`
- `cargo test -p md_editor`
- `cargo perf-test -p md_editor`

重点看这些 perf 结果：

- 短 Markdown 第一次滑动。
- 短 Markdown 第二次滑动。
- 300KB Markdown 第一次滑动。
- 300KB Markdown 第二次滑动。
- Source 模式和 Rendered 模式分别统计。

理想结果：

- 300KB 第二次滑动应接近短文档第二次滑动。
- 300KB 第一次滑动应随着预计算推进而明显下降。
- Source 模式先达到稳定；Rendered 模式在合并 Markdown 查询后继续下降。
